import type { HabitTracking, Sphere, Importance } from '../../lib/life/types'
import { SPHERES, IMPORTANCE } from '../../lib/life/model'
export function HabitTrackingFields({value,onChange,locked=false,habits=[]}:{value:HabitTracking;onChange:(value:HabitTracking)=>void;locked?:boolean;habits?:{id:string;name:string}[]}) {
 const patch=(p:Partial<HabitTracking>)=>onChange({...value,...p})
 return <details className="life-form mt-3 rounded-xl bg-canvas p-3"><summary className="font-bold text-ink">Направление, количество и сфера</summary><div className="mt-3 space-y-3"><label>Направление<select value={value.intent??'develop'} disabled={locked} onChange={e=>patch({intent:e.target.value as 'develop'|'reduce'})}><option value="develop">Развиваю</option><option value="reduce">Сокращаю</option></select></label>{locked&&<p className="life-muted">Направление квестовой привычки меняется после завершения контракта.</p>}{value.intent==='reduce'&&<><label>Дневной предел, необязательно<input type="number" min="0" value={value.limit??''} onChange={e=>patch({limit:e.target.value===''?undefined:Math.max(0,Number(e.target.value))})}/></label><label>Альтернатива<input list="habit-alternatives" value={value.alternative??''} onChange={e=>patch({alternative:e.target.value})}/><datalist id="habit-alternatives">{['Чтение','Прогулка','Спорт','Дневник','Медитация','Обучение'].map(x=><option key={x} value={x}/>)}</datalist></label><label>Или существующая привычка<select value={value.alternativeHabitId??''} onChange={e=>patch({alternativeHabitId:e.target.value||undefined,alternative:habits.find(h=>h.id===e.target.value)?.name??value.alternative})}><option value="">Собственное действие</option>{habits.map(h=><option key={h.id} value={h.id}>{h.name}</option>)}</select></label><p className="life-muted">Для предела длительности укажи единицу «мин», для частоты — «раз».</p></>}<div className="grid grid-cols-2 gap-2"><label>Цель в день<input type="number" min="1" step="any" value={value.quantityTarget??''} placeholder="1" onChange={e=>patch({quantityTarget:Number(e.target.value)>0?Number(e.target.value):undefined})}/></label><label>Единица<input value={value.unit??''} placeholder="мин, страницы, раз" onChange={e=>patch({unit:e.target.value})}/></label></div><label>Сфера<select value={value.sphere??''} onChange={e=>patch({sphere:(e.target.value||undefined) as Sphere})}><option value="">Пока не выбрана</option>{Object.entries(SPHERES).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select></label><label>Категория<input value={value.category??''} onChange={e=>patch({category:e.target.value})}/></label><label>Важность<select value={value.importance??'personal'} onChange={e=>patch({importance:e.target.value as Importance})}>{Object.entries(IMPORTANCE).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select></label><label>Теги через запятую<input value={value.tags?.join(',')??''} onChange={e=>patch({tags:e.target.value.split(',')})}/></label></div></details>
}
